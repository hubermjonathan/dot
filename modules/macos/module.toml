[module]
name = "macos"
description = "macOS system preferences"

[links]
"wallpaper.jpg" = "~/Pictures/wallpaper.jpg"

[deps]
brew = ["desktoppr", "mysides"]

[health]
checks = [
  "file_exists:/etc/pam.d/sudo_local",
  "command_succeeds:grep -E '^[[:space:]]*auth[[:space:]]+sufficient[[:space:]]+pam_tid.so' /etc/pam.d/sudo_local",
]

[setup]
interactive = true
post_link = [
  "if grep -qE '^[[:space:]]*auth[[:space:]]+sufficient[[:space:]]+pam_tid.so' /etc/pam.d/sudo_local 2>/dev/null; then echo 'Touch ID for sudo already configured, skipping...'; else echo 'Configuring Touch ID for sudo (requires password)...'; printf 'auth       sufficient     pam_tid.so\\n' | sudo tee -a /etc/pam.d/sudo_local >/dev/null; fi",
  "osascript -e 'tell application \"System Events\" to tell appearance preferences to set dark mode to true'",
  "defaults write com.apple.dock autohide -bool true",
  "defaults write com.apple.dock magnification -bool false",
  "defaults write com.apple.dock persistent-apps -array",
  "defaults write com.apple.dock persistent-others -array",
  "defaults write com.apple.dock recent-apps -array",
  "defaults write com.apple.dock show-recents -bool false",
  "defaults write com.apple.dock wvous-tl-corner -int 0",
  "defaults write com.apple.dock wvous-tr-corner -int 0",
  "defaults write com.apple.dock wvous-bl-corner -int 0",
  "defaults write com.apple.dock wvous-br-corner -int 4",
  "defaults write com.apple.menuextra.clock ShowSeconds -bool true",
  "defaults write com.apple.WindowManager showStandardWidgets -bool false",
  "defaults write com.apple.WindowManager showStageManagerWidgets -bool false",
  "python3 -c \"import plistlib,subprocess,tempfile,os; r=subprocess.run(['defaults','export','com.apple.notificationcenterui','-'],capture_output=True); p=plistlib.loads(r.stdout); p['widgets']={'instances':[],'vers':1}; f=tempfile.NamedTemporaryFile(suffix='.plist',delete=False); plistlib.dump(p,f); f.close(); subprocess.run(['defaults','import','com.apple.notificationcenterui',f.name]); os.unlink(f.name)\"",
  "defaults write NSGlobalDomain com.apple.swipescrolldirection -bool false",
  "defaults write NSGlobalDomain com.apple.keyboard.fnState -bool true",
  "defaults write NSGlobalDomain NSAutomaticCapitalizationEnabled -bool false",
  "defaults write NSGlobalDomain NSAutomaticPeriodSubstitutionEnabled -bool false",
  "defaults write com.google.Chrome NSUserKeyEquivalents -dict-add 'New Tab to the Right' '@^t'",
  # finder
  "defaults write com.apple.finder NewWindowTarget -string PfLo",
  "defaults write com.apple.finder NewWindowTargetPath -string \"file://$HOME/Downloads/\"",
  "defaults write com.apple.finder FXRemoveOldTrashItems -bool true",
  "for item in 'file:///Users/$USER/Recents/' 'nwnode://domain-AirDrop' 'file:///Network/' 'TagsItem-Group'; do mysides remove \"$item\" >/dev/null 2>&1 || true; done",
  "for tag in red orange yellow green blue purple gray; do mysides remove \"$tag\" >/dev/null 2>&1 || true; done",
  "mysides add Pictures \"file://$HOME/Pictures/\" >/dev/null",
  "/System/Library/PrivateFrameworks/SystemAdministration.framework/Resources/activateSettings -u",
  "desktoppr ~/Pictures/wallpaper.jpg",
  "sudo dscl . delete /Users/$USER JPEGPhoto 2>/dev/null || true",
  "sudo dscl . create /Users/$USER Picture ~/Pictures/profile-picture.jpg",
  "killall Dock",
  "killall Finder",
  "killall NotificationCenter",
  "killall SystemUIServer",
  "killall WindowManager",
]
